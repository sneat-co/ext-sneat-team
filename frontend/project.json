{
  "name": "ext-sneat-team-contract",
  "$schema": "./node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "src",
  "projectType": "library",
  "tags": ["layer:contract", "domain:team"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/dist"],
      "options": { "command": "pnpm build" }
    },
    "nx-release-publish": {
      "options": { "packageRoot": ".", "access": "public" }
    }
  }
}
